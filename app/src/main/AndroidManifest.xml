<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testnet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StartActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".TeacherMenuActivity" />
        <activity android:name=".StudentMenuActivity" />
        <activity android:name=".SingleStudentGradesActivity" />
        <activity android:name=".JoinExamActivity" />
        <activity android:name=".TakeExamActivity" />
        <activity android:name=".CreateExamActivity" />
        <activity android:name=".ExamForResultsActivity" />
        <activity android:name=".AllStudentsGradesActivity" />

    </application>

</manifest>