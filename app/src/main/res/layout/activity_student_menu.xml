<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".StudentMenuActivity">

    <Button
        android:id="@+id/takeExamBtn"
        style="@style/ButtonStyle"
        android:layout_width="112dp"
        android:layout_height="41dp"
        android:layout_marginTop="196dp"
        android:onClick="takeExamClicked"
        android:text="@string/take_an_exam"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/watchGradesBtn"
        style="@style/ButtonStyle"
        android:layout_width="94dp"
        android:layout_height="37dp"
        android:layout_marginTop="28dp"
        android:onClick="watchGradesClicked"
        android:text="@string/my_grades"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/takeExamBtn" />

    <Button
        android:id="@+id/studentMenuLogoutBtn"
        style="@style/ButtonStyle"
        android:layout_width="84dp"
        android:layout_height="35dp"
        android:layout_marginBottom="44dp"
        android:onClick="logoutClicked"
        android:text="@string/logout"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.157"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>